name: murasame_flutter_chat
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # package_import_sort
  import_sorter: ^4.6.0

  # Firebase
  firebase_core: ^1.12.0
  firebase_auth: ^3.3.7
  cloud_firestore: ^3.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # State Management
  flutter_hooks: ^0.18.2+1
  hooks_riverpod: ^1.0.3

  # linter
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
